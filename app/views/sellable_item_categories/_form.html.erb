<%= form_for(@sellable_item_category) do |f| %>
  <% if @sellable_item_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sellable_item_category.errors.count, "error") %> prohibited this sellable_item_category from being saved:</h2>

      <ul>
      <% @sellable_item_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
